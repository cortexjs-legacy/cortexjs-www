include ./mixin/component

extends layout/default.jade

block head  
	title cortex
	+css("/css/package.css")
	+css("/css/lib/readme.css")

block content  
	+nav(user)
	section.container
		.top-area
			span.info #{pkg.name}
			select.versions
				each detail, version in pkg.versions
					option #{version}
			span.info-box #{pkg.license}
		.detail
			.row
				.col
					+package-stat(pkg)
					+command("install","npm install "+ pkg.name +" --save")
					+maintainers(pkg.maintainers)
				.col
					.dependency-info.card
						.tabs
							a.tab.active(data-target="dependency") DEPENDENCIES
							a.tab(data-target="dev") DEV
							a.tab(data-target="dependents") DEPENDENTS
						.content.tab-pane.active(data-content="dependency")
							table
								tr
									th FULL NAME
									th REQUIREMENT
								each version, dependency in pkg.dependencies
									tr
										td #{dependency}
										td #{version}
						.content.tab-pane(data-content="dev")
							table
								tr
									th FU NAME
									th REQUIREMENT
								each version, dependency in pkg.dependencies
									tr
										td #{dependency}
										td #{version}
						.content.tab-pane(data-content="dependents")
							table
								tr
									th FULL NA
									th REQUIREMENT
								each version, dependency in pkg.dependencies
									tr
										td #{dependency}
										td #{version}

			.row
				section.readme.card
					.title README
					.content.markdown !{pkg.readme}
	script.
		facade({
			mod: 'cortexjs-www/entries/package'
		});


