include ./mixin/component

extends layout/default.jade

block head  
	title cortex
	+css("./css/search.css")

block content  
	+nav(user)
	section.container
		.top-area
			span.info #{pkgs.length} pacakges about
				b  #{keyword}
		.packages
			each pkg in pkgs
				+package-info(pkg)


